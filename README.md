# Knollege
Year 3 project 



V1.0:
Must install nodes with "npm install package.json", if having issues with MongoDB not recongized: use "npm install mongodb" aswell.
In MongoDB (user: admin, password: pass): create database "app", collection "notes", document with {"_id": 100, "text": " "}
